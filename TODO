
Legend:

o a pending  TODO item (normal priority)
. a pending  TODO item (low priority)
x a finished TODO item

-----------------------------------------------------------------------------

x remove win32 compiler warnings (related to __FUNCTION__)

x need new internal hamster function: add_context_data, get_context_data
    x unittests

x add documentation fixes

x change the interface of the callback functions (prefix-compare
    and normal compare); need an additional "db" parameter!

o release 1.0.0!

-----------------------------------------------------------------------------

o merge changes of 1.0 with the trunk

o define a new release process for stable/unstable releases

o new web page

o commercial licensing

o java wrapper
    x package name is de.crupp.hamsterdb (??)
    x create directory structure - bindings/java/..., src/java
    x add autoconf stuff 
        x search jni.h
        x --enable-java
        x --with-jdk=...
    x implementation (synchronized!)
        x key
        x record
        x error
        x database
        x cursor
        x environment
    x synchronization problem - currently, Cursor and Database have
        their own mutex; instead, Cursor must use the mutex of its
        Database!
    x new class Const for flags etc
    x get rid of Key and Record and replace them with byte[]-arrays
    x Environment::getDatabaseNames() is losing an error code!
    x add header comments/copyright
    x ham_cursor_move returns 3 parameters, 2 of them are optional - but how?
    o add unittests for each function and code path
        x const
        x error
        x database
        x cursor
        o environment
    x make sure that throwing exceptions really works!
    x more robustness in the jni code!! - error checks etc
    o add 2 samples (db1.java, env2.java)
    o add autoconf build automization
    o test with valgrind
    o add win32 build automization
    o add the full documentation!!
    o create jar

    o add testing scheme to release process
        o win32
        o win64
        o linux 32bit
        o linux 64bit

    o extend release process for java

o .NET wrapper

o PHP wrapper

o Python wrapper

o Perl wrapper

-----------------------------------------------------------------------------

java callback-comparison:
    libdb_java/db_java_wrap.c funktion __dbj_bt_compare

. tests are failing because of a berkeleydb bug:
    --duplicate=1 --use-cursors=1
      o ../../testfiles/1/100.tst - status mismatch
      o ../../testfiles/1/202.tst - status mismatch
      o ../../testfiles/1/220.tst - status mismatch

