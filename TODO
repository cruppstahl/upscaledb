
Legend:

o a pending  TODO item (normal priority)
. a pending  TODO item (low priority)
x a finished TODO item

-----------------------------------------------------------------------------

x merge changes of 1.0 with the trunk

x .NET wrapper
    x add the documentation
    x completely automize the build process
    x first release 0.0.1

o new web page
    x upload the samples
    x upload the tutorials
    x upload all files (download page!)
    x update download page (1.0!)
    x update news (fix links and add 1.0!)
    x update link cloud (1.0!)
    x upload doxygen-documentation
    x check all links with a recursive link checker
    x contact: noch ein "company" und (optionales) "address"-feld?
    x redesign front-page (new picture)
    x everywhere: "embedded database" instead of "embeddable database"
    x frontpage: "embedded database" instead of "embeddable database"
    x IE tests
    o add licensing stuff
    o download-page: we now have several products, and they even have
        dependencies! restructure everything
        - on a second page, list all releases (incl. older releases - 
        automatically generated from the filelist, or from an XML file)
    o online-shop with Paypal integration
    o add google analytics code

o java wrapper
    o completely automize the build process

-----------------------------------------------------------------------------

. Python wrapper 
    -> http://docs.python.org/ext/ext.html
    -> http://www.python.org/doc/faq/extending/

. Perl wrapper

. PHP wrapper

-----------------------------------------------------------------------------

. tests are failing because of a berkeleydb bug:
    --duplicate=1 --use-cursors=1
      o ../../testfiles/1/100.tst - status mismatch
      o ../../testfiles/1/202.tst - status mismatch
      o ../../testfiles/1/220.tst - status mismatch

