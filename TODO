
x make sure that --mmap=0 really disables mmap (getopt-problem?)

o new flag HAM_LOCK_EXCLUSIVE
    x implementation
        x for linux/posix
        x for win32
    x documentation
    o test
        x linux
        o windows

o move blobid handling of extended keys to its own function

o write more unittests

o fix ALL compiler warnings
    x gcc 4.x
    o gcc 3.x
    o msvc

o port to QNX (?)

o allow x64-bit builds with MSVC/Visual Studio Solution file

o release 0.4.4!

