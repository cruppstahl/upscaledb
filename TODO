
Legend:

o a pending  TODO item (normal priority)
. a pending  TODO item (low priority)
x a finished TODO item

-----------------------------------------------------------------------------

o hamsterdb.com web page
    . add RSS/Atom feed
    . add Google sitemap
    . add documentation for Java
    . add documentation for .NET (chm file)

x ham_close: also support HAM_TXN_AUTO_COMMIT
    x unittest

x ham_env_close: also support HAM_TXN_AUTO_COMMIT
    x test

x enhance C++ wrapper for transactions

x when checking extended parameters: make sure that max. 0xffff keys fit 
    in a node (see hamsterdb forum)
    x unittest

x linux: use O_NOATIME flag (see http://kerneltrap.org/node/14148)

o tools/tests/test.sh: add to release-process
    - tricky: currently fails because of invalid database versions, and
        because the pagesize is different on cygwin

. enhance tutorial with information about transactions
    o new CI!

. new flag HAM_CACHE_UNLIMITED - do never purge the cache. should bring
    the performance of in-memory to a file-based database.
    o does not work with HAM_CACHE_STRICT or a cache size

. unittests: get rid of cppunit, move to bfc
    o update configure.in
    o update win32 project file
    o update wince project file
    o update hamsterdb-alien
    o finally run tests in win64
    o finally run tests in wince

. enhance java/python/.NET for transactions

. enhance acceptance tests - will need new test scripts
    TXN_BEGIN 1  (1 is the txn id)
    TXN_ABORT 1
    TXN_INSERT 1 ...
    TXN_ERASE 1 ...
    TXN_COMMIT 1

. move transactions to environment, not database -- or not? if it works, then
    maybe leave it as is

. release-process: enhance automated tests of win32/win64 dll and lib

. don't make ham_env_erase_db fail if a txn is in progress

. allow more than one txn in parallel

-----------------------------------------------------------------------------

. Perl wrapper

. PHP wrapper

. new configure switch --force-lsb-compliancy (ohne pread, pwrite)
    o needs support for automated testing!

-----------------------------------------------------------------------------

. tests are failing because of a berkeleydb bug:
    --duplicate=1 --use-cursors=1
      o ../../testfiles/1/100.tst - status mismatch
      o ../../testfiles/1/202.tst - status mismatch
      o ../../testfiles/1/220.tst - status mismatch

