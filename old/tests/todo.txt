NOCH ZU ERLEDIGEN --------------------------

3. delete() implementieren

8. testen mit löschen von links, von rechts, zufälliges löschen; verschiedene
MINKEY/MAXKEY-Werte und pagegrössen testen!

-----------------------

246 585 596 595 694 105 524 304 577 591 913 571 453 180 857 849 211 268 764 168 351 405 916 357 206 576 715 78 684 222 290 648 525 885 244 937 709 767 960 5 358 873 810 771 152 378 981 419 861 868 769 266 503 126 

-----------------------

verify: assert() aktivieren (page->count>=MINKEYS)!

-----------------------

6. die memory-leaks müssen raus - erst mit free(), dann später durch
my_cache_pushback() (oder page_release() o.ä.) 
in allocPage() und unallocPage() einen zaehler machen - allocPage(): zaehler++,
unallocPage(): zaehler--; am schluss muss der zaehler bei 0 sein!

7. iteratoren bzw cursors

SCHON ERLEDIGT -----------------------------

1. insert() und find() testen - mit zufallszahlen im range (0, 1024[ - in
bool-array wird markiert, welcher wert eingefügt wurde, mit find() kann dann
überprüft werden, ob das ergebnis korrekt gefunden wird

2. testprogramm, das eine datei zeilenweise abarbeitet; jede zeile besteht
aus integern, die nacheinander eingefügt werden; der baum, der am ende besteht,
wird ausgegeben.

4. regression tests - speichern key-sequenzen, time(1)-ausgaben, grafisches
bild des ausgegebenen baums, versionsnummer/build-nr des btree-testlaufs; 
vergleicht dann die ergebnisse mit vorherigen durchläufen -> bei fehlern 
wird gekotzt
    parameter: btree --quiet --noninteractive
               btree --insert <file>
                    einfügen, find, verify, dump speichern, erase
               btree --random

    datenbank: indiziert durch 
        testfile -> buildno -> time(1)-ausgabe
                               dump()-ausgabe
                               timestamp

    tests werden durchlaufen, stdout wird geparst. bei abweichungen wird der
    test nicht in der DB gespeichert, es wird angehalten und der user wird
    gefragt ob der test wiederhold werden soll (im interaktiven modus).

    ausgabe:
    
        test-file: assert001.txt
                    old     current     diff        
        real        0.312   0.512       +0.200      !!
        user        0.108
        sys         0.035

        durch das !! wird angezeigt, dass hier eine signifikante abweichung
        vorliegt (>5% unterschied zum SCHNELLSTEN test!)

5. bei option "-a 1 2 3 -2" versucht das testprogramm, 2 zu finden und
bekommt deshalb natürlich einen fehler.

8. bei den testreihen müssen die negativen werte auch berücksichtigt werden



